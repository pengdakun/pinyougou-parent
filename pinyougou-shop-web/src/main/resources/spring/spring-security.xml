<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- 配置login.html不登录也可以访问 -->
	<http pattern="/*.html" security="none"></http>
	<http pattern="/css/**" security="none"></http>
	<http pattern="/img/**" security="none"></http>
	<http pattern="/js/**" security="none"></http>
	<http pattern="/plugins/**" security="none"></http>
	<http pattern="/seller/add.do" security="none"></http>

	<!-- 配置页面拦截规则   use-expressions是否启用SPEL表达式 -->
	<http use-expressions="false">
		<!-- 当前用户必须有ROLE_USER的角色，才可以访问根目录及所属子目录资源 -->
		<intercept-url pattern="/**" access="ROLE_SELLER"/>
		<!-- 开启表单登录功能,可以自动生成简单登录页面     always-use-default-target配置登录成功一直跳转到default-target-url-->
		<form-login login-page="/shoplogin.html" default-target-url="/admin/index.html" authentication-failure-url="/shoplogin.html" always-use-default-target="true"/>
		<!-- 关闭安全认证 -->
		<csrf disabled="true"/>
		<headers>
			<!-- 配置可以加载框架页，iframe等 -->
			<frame-options policy="SAMEORIGIN"/>
		</headers>
		<!-- 退出登录，请求地址/logout -->
		<logout/>
	</http>
	
	<!-- 认证管理器 -->
	<authentication-manager>
		<!-- 认证提供者 -->
		<authentication-provider user-service-ref="userDetailsServiceImpl">
			<password-encoder ref="bCryptPasswordEncoder"></password-encoder>
		</authentication-provider>
	</authentication-manager>
	
	<!-- 配置认证类 -->
	<beans:bean id="userDetailsServiceImpl" class="com.peng.pinyougou.shop.service.UserDetailsServiceImpl">
		<beans:property name="sellerService" ref="sellerService"></beans:property>
	</beans:bean>
	
	<!-- 引用dubbo 服务 -->
	<dubbo:application name="pinyougou-shop-web"/>
	<dubbo:registry address="zookeeper://192.168.25.128:2181"/>
	<dubbo:reference id="sellerService" interface="com.peng.pinyougou.sellergoods.service.SellerService"></dubbo:reference>
	
	<!-- 配置加密类 -->
	<beans:bean id="bCryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"></beans:bean>
	
</beans:beans>